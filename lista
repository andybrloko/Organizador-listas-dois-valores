<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Organizador de Lista</title>

    <link rel="stylesheet" href="https://pyscript.net/latest/pyscript.css" />
    <script defer src="https://pyscript.net/latest/pyscript.js"></script>
</head>

<body>

<h1>Organizador de Nomes e Valores</h1>

<p>Digite os nomes (separados por espaço):</p>
<input id="nomes" type="text" style="width: 400px;">

<p>Digite os valores (separados por espaço):</p>
<input id="valores" type="text" style="width: 400px;">

<br><br>
<button py-click="organizar()">Organizar</button>

<h2>Resultado:</h2>
<pre id="saida"></pre>

<py-script>
def organizar():
    # Recebe valores dos campos HTML
    listaN = Element("nomes").value.split()
    listaV = Element("valores").value.split()

    # Verificações iguais às do seu código
    if not listaN:
        Element("saida").write("não há nada na lista de nomes")
        return
    if not listaV:
        Element("saida").write("não há nada na lista de valores")
        return

    # Associa nomes e valores
    dados_ass = dict(zip(listaN, listaV))

    # Ordena do maior para o menor
    ordenado = sorted(dados_ass.items(), key=lambda x: int(x[1]), reverse=True)

    # Monta saída igual ao console do VS Code
    texto = "nome                        valor\n"
    texto += "-" * 35 + "\n"

    for nome, valor in ordenado:
        texto += f"{nome:<20} {valor:>10}\n"

    Element("saida").write(texto)
</py-script>

</body>
</html>
